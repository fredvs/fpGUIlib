Copy fpGUI library (there are some pre-compiled in /bin_fpGUIlib) into the /demo_xxx folder you want to compile.

Python and fpc need native-libraries.
Java needs Java-libraries.

Is you want assistive, copy /sakit into the demo_xxx folder.
